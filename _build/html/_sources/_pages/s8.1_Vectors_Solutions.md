

# Vectors

In this week's tutorial, we will use the techniques from the previous lecture to complete a mini-project. You should save any files you work on as spreadsheets, so you can refer back to them later.

```{exercise}
:label: vecs-ex1-sol

(a) Plot the vectors given below on a pair of axes. (Graph paper will be available).

$$\mathbf{a} = (2, 2) \qquad
\mathbf{b} = (4, 2) \qquad
\mathbf{c} = (-3, 1) \qquad
\mathbf{d} = (1, -2)$$
    
(b) List the possible pairs of vectors from this set.

(c) The dot product of a pair of vectors measures the extent to which those two vectors are pointing towards the same place. Without calculating them, predict the ordering of the dot products of each of the pairs (from the largest negative value to the largest positive value). Then, confirm your calculations. 

```

```{exercise}
:label: vecs-ex2-sol

No solutions here (exploratory only)


```

```{exercise}
:label: vecs-ex3-sol


(b) 

&emsp;(i) $(1,2,3) \cdot (4,−5,6) = 4 − 10 + 18 = 12.$

&emsp;(ii) $(−4,−9) \cdot (−1,2) = = 4 − 18 = −14.$

&emsp;(iii) $\mathbf{a}\cdot \mathbf{b} =(6 \times 4) + (−1 \times c)+(3 \times (−2))=24−c−6=18−c$
In order for them to be perpendicular we need $\mathbf{a} \cdot \mathbf{b} = 0 = 18-c$, or $c=18$.

Double check: $\mathbf{a} \cdot \mathbf{b} =(6,−1,3)\cdot(4,18,−2)=0$.
.